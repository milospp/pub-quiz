let configData = {
    "AUTH_SERVICE_URL": `http://${document.domain}:8000`,
    "QUIZ_SERVICE_URL": `http://${document.domain}:8001`,
    "QUIZ_SOCKET_SERVICE_URL": `ws://${document.domain}:8002`
}

export default configData